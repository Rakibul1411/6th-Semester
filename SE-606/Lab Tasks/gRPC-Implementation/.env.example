# Environment Configuration
NODE_ENV=development

# gRPC Server Configuration
GRPC_HOST=0.0.0.0
GRPC_PORT=50051

# HTTP Server Configuration (for health checks)
HTTP_PORT=3000

# Redis Configuration (optional - will use in-memory fallback if not available)
REDIS_HOST=localhost
REDIS_PORT=6379

# MongoDB Configuration (already set in code, but can be overridden)
# MONGODB_URI=mongodb+srv://mdrakibul11611:mdrakibul11611@usermanagement.cnqosei.mongodb.net/?retryWrites=true&w=majority&appName=UserManagement

# Logging Configuration
LOG_LEVEL=info

# Queue Configuration
QUEUE_RETRY_ATTEMPTS=3
QUEUE_RETRY_DELAY=2000
